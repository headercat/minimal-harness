{
  "name": "minimal-harness",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -ws --if-present",
    "typecheck": "npm run typecheck -ws --if-present",
    "dev": "npm run dev -w @headercat/minimal-harness",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.4",
    "typescript": "^5.7",
    "@ai-sdk/openai-compatible": "^2.0.48",
    "ai": "^6.0.197",
    "tsx": "^4.19",
    "zod": "^4.4.3"
  }
}
